import Form from "./Form";

const Projects = () => {
  return (
    <section
      className="section static md:relative fp-auto-height 
    bg-bg-pattern  min-h-full  md:min-h-screen  bg-contain bg-left-top bg-no-repeat ">
      <Form />
    </section>
  );
};

export default Projects;
